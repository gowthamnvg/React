name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - cablenetwork  # Change this to your branch name

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 20.10.x  # You can change the version if needed

    - name: Install Dependencies
      run: npm install

    - name: Build
      run: npm run build  # Adjust the script if needed

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build
